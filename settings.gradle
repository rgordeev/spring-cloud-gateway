/*
 * This file was generated by the Gradle 'init' task.
 */

rootProject.name = 'spring-cloud-gateway'

include "bookstore"
project(':bookstore').projectDir = new File(rootProject.projectDir, "bookstore")

include "discovery"
project(':discovery').projectDir = new File(rootProject.projectDir, "discovery")

include "gateway"
project(':gateway').projectDir = new File(rootProject.projectDir, "gateway")

rootProject.children.each { project ->
    assert project.projectDir.isDirectory()
}
